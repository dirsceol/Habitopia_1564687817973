/* 
* Generated by
* 
*      _____ _          __  __      _     _
*     / ____| |        / _|/ _|    | |   | |
*    | (___ | | ____ _| |_| |_ ___ | | __| | ___ _ __
*     \___ \| |/ / _` |  _|  _/ _ \| |/ _` |/ _ \ '__|
*     ____) |   < (_| | | | || (_) | | (_| |  __/ |
*    |_____/|_|\_\__,_|_| |_| \___/|_|\__,_|\___|_|
*
* The code generator that works in many programming languages
*
*			https://www.skaffolder.com
*
*
* You can generate the code from the command-line
*       https://npmjs.com/package/skaffolder-cli
*
*       npm install -g skaffodler-cli
*
*   *   *   *   *   *   *   *   *   *   *   *   *   *   *   *
*
* To remove this comment please upgrade your plan here: 
*      https://app.skaffolder.com/#!/upgrade
*
* Or get up to 70% discount sharing your unique link:
*       https://app.skaffolder.com/#!/register?friend=5d41c8dfc0e2910e77977c56
*
* You will get 10% discount for each one of your friends
* 
*/
// Import Libraries
import { Component, OnInit } from '@angular/core';
import { Location } from '@angular/common';
import { ActivatedRoute } from '@angular/router';
import { AngularFirestore, AngularFirestoreDocument, AngularFirestoreCollection } from '@angular/fire/firestore';
import { Observable } from 'rxjs';

// Import Services
import { CueService } from '../../services/cue.service';
import { HabitService } from '../../services/habit.service';

import { Cue } from '../../domain/habitopia_db/cue';
import { Habit } from '../../domain/habitopia_db/habit';

// START - USED SERVICES
/**
* CueService.create
*	@description CRUD ACTION create
*
* HabitService.findBycue
*	@description CRUD ACTION findBycue
*	@param Objectid key Id della risorsa cue da cercare
*
* CueService.get
*	@description CRUD ACTION get
*	@param ObjectId id Id 
*
* HabitService.list
*	@description CRUD ACTION list
*
* CueService.update
*	@description CRUD ACTION update
*	@param ObjectId id Id
*
*/
// END - USED SERVICES

/**
 * This component allows to edit a  Cue
 */
@Component({
    selector: 'app-cue-edit',
    templateUrl: 'cue-edit.component.html',
    styleUrls: ['cue-edit.component.css']
})
export class CueEditComponent implements OnInit {
    item: any = {};
    itemDoc: AngularFirestoreDocument<Cue>;
    isNew: Boolean = true;
    formValid: Boolean;

    listCue: Cue[];
    listPreceedingHabit: Habit[];

    externalHabit: Habit[];

    constructor(
        private cueService: CueService,
        private habitService: HabitService,
        private route: ActivatedRoute,
        private location: Location) {
        // Init list
        this.externalHabit = [];
    }

    /**
     * Init
     */
    ngOnInit() {
        this.route.params.subscribe(param => {
            const id: string = param['id'];
            if (id !== 'new') {
                this.isNew = false;
                this.itemDoc = this.cueService.get(id);
                this.itemDoc.valueChanges().subscribe(item => this.item = item);

                this.habitService.findByCue(id).subscribe(list => this.externalHabit = list);
            }
            // Get relations
            this.habitService.list().subscribe(list => this.listPreceedingHabit = list);
        });
    }



    /**
     * Save Cue
     *
     * @param {boolean} formValid Form validity check
     * @param Cue item Cue to save
     */
    save(formValid: boolean): void {
        this.formValid = formValid;
        if (formValid) {
            if (this.isNew) {
                // Create
                this.cueService.create(this.item);
                this.isNew = false;
            } else {
                // Update
                this.cueService.update(this.itemDoc, this.item);
            }
            this.goBack();
        }
    }

    /**
     * Go Back
     */
    goBack(): void {
        this.location.back();
    }

}
